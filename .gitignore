# Build intermediates from the Hugo→Mintlify conversion (kinetica-docs/mintlify).
# Per-API docs.json nav fragments: emitted by build_api_docs.sh and merged into
# docs.json by generate_config.py. Not served (dot-prefixed) and not needed at
# runtime — regenerated on every build.
.api-nav/
