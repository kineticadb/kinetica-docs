---
title: /delete/directory
---

{/* RST directive: role h2_style */}


```
URL: http://<db.host>:<db.port>/delete/directory
```
Deletes a directory from [KiFS](../../tools/kifs/).

## Input Parameter Description

<ParamField body="directory_name" type="string">

Name of the directory in KiFS to be deleted. The directory must contain no files, unless *recursive* is *true*.

</ParamField>

<ParamField body="options" type="map of string to strings">

Optional parameters.

The default value is an empty map ( \{\} ).

<Expandable title="options">

<ParamField body="recursive">

If *true*, will delete directory and all files residing in it. If false, directory must be empty for deletion.

The default value is `false`.

The supported values are:

- true
- false

</ParamField>

<ParamField body="no_error_if_not_exists">

If *true*, no error is returned if specified directory does not exist.

The default value is `false`.

The supported values are:

- true
- false

</ParamField>

</Expandable>

</ParamField>


## Output Parameter Description

The Kinetica server embeds the endpoint response inside a standard response structure which contains status information and the actual response to the query.  Here is a description of the various fields of the wrapper:

<ResponseField name="status" type="String">

'OK' or 'ERROR'

</ResponseField>

<ResponseField name="message" type="String">

Empty if success or an error message

</ResponseField>

<ResponseField name="data_type" type="String">

'delete_directory_response' or 'none' in case of an error

</ResponseField>

<ResponseField name="data" type="String">

Empty string

</ResponseField>

<ResponseField name="data_str" type="JSON or String">

This embedded JSON represents the result of the /delete/directory endpoint:

<Expandable title="data_str">

<ResponseField name="directory_name" type="string">

Value of input parameter *directory_name*.

</ResponseField>

<ResponseField name="info" type="map of string to strings">

Additional information.

</ResponseField>

</Expandable>

Empty string in case of an error.

</ResponseField>
